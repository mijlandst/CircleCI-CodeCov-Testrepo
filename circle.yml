general:
  build_dir: code

machine:
  java:
    version: openjdk8
    
test:
  post:
    - "bash <(curl -s https://codecov.io/bash)"
    - "cat target/site/jacoco/index.html"
    - "cat target/site/jacoco/index.xml"
    - "cat target/site/jacoco/index.csv"
  
